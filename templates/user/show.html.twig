{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
  	<div class="row">
		<div class="col-md-11 mx-auto mb-5">
			<h1><u>Détail d'un utilisateur</u></h1>
		</div>
	</div>
    	<div class="row">
		<div class="col-md-11 mx-auto">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{user.technicien.nom}}</td>
            </tr>
             <tr>
                <th>Prénom</th>
                <td>{{user.technicien.prenom}}</td>
            </tr>
             <tr>
                <th>Métier</th>
                <td>{{user.technicien.metier}}</td>
            </tr>
             <tr>
                <th>Roles</th>
                <td>{% for role in user.roles%} {{ role}},  {% endfor %}</td>
            </tr>
            
        </tbody>
    </table>
<div class="row">
<div class="colmd-11 mx-auto">

<a class="btn btn-sm btn-primary" href="{{ path('user_index') }}">Retour à la liste</a>

    <a class="btn btn-sm btn-primary"  href="{{ path('user_edit', {'id': user.id}) }}">Edition</a>
{{ include('user/_delete_form.html.twig') }}
</div></div>
    
    </div>
    </div>

    
{% endblock %}
