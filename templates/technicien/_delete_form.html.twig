<form method="post" action="{{ path('technicien_delete', {'id': technicien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technicien.id) }}">
    <button class="btn">Delete</button>
</form>
